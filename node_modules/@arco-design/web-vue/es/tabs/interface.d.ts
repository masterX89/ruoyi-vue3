import type { RenderFunction } from 'vue';
export declare const POSITIONS: readonly ["left", "right", "top", "bottom"];
export declare type Positions = typeof POSITIONS[number];
export declare const TYPES: string[];
export declare type Types = typeof TYPES[number];
export interface TabData {
    key: string;
    title: string;
    disabled?: boolean;
    closable?: boolean;
}
export declare type TabList = Array<{
    key: string;
    title: RenderFunction;
    disabled: boolean;
    closable: boolean;
}>;
