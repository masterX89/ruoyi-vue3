import { Ref } from 'vue';
export default function usePopupManager({ visible, runOnMounted, }?: {
    visible?: Ref<boolean>;
    runOnMounted?: boolean;
}): {
    id: string;
    zIndex: import("vue").ComputedRef<number>;
    nextZIndex: () => number;
    close: () => void;
    open: () => void;
};
