import { TableProps } from '../interface';
export declare const useExpand: (props: TableProps, allRowKeys: string[], emit: any) => {
    expandedRowKeys: import("vue").ComputedRef<string[]>;
    handleExpand: (rowKey: string) => void;
};
