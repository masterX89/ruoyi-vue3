import { isObject } from "../_utils/is.js";
const getValueData = (value) => value.map((item) => {
  if (isObject(item))
    return item;
  return {
    value: item,
    label: String(item),
    closable: true
  };
});
export { getValueData };
