import { VNode } from 'vue';
import { GridItemProps, GridItemVNode, GridItemData } from '../interface';
export declare function getGridItemChildren(children: VNode[]): GridItemVNode[];
export declare function resolveItemData(cols: number, props: Partial<GridItemProps>): GridItemProps;
export declare function setItemVisible({ cols, collapsed, collapsedRows, itemData, }: {
    cols: number;
    collapsed: boolean;
    collapsedRows: number;
    itemData: GridItemData[];
}): {
    overflow: boolean;
    displayItemData: GridItemData[];
};
