import { defineComponent } from "vue";
var GridItem = defineComponent({
  name: "GridItem",
  props: {
    span: Number,
    offset: Number,
    suffix: Boolean
  }
});
export { GridItem as default };
