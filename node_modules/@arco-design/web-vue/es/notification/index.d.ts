import type { App } from 'vue';
import { NotificationConfig } from './interface';
declare const Notification: {
    install: (app: App) => void;
    info: (config: string | NotificationConfig) => import("./interface").NotificationReturn;
    success: (config: string | NotificationConfig) => import("./interface").NotificationReturn;
    warning: (config: string | NotificationConfig) => import("./interface").NotificationReturn;
    error: (config: string | NotificationConfig) => import("./interface").NotificationReturn;
    clear: (position?: "topLeft" | "topRight" | "bottomLeft" | "bottomRight" | undefined) => void;
};
export default Notification;
