import { Mode, FormatFunc } from '../interface';
interface FormatProps {
    format: string | FormatFunc | undefined;
    mode?: Mode;
    showTime?: boolean;
}
export declare function getDefaultFormat(mode?: Mode, showTime?: boolean): "gggg-wo" | "YYYY-MM" | "YYYY" | "YYYY-[Q]Q" | "YYYY-MM-DD HH:mm:ss" | "YYYY-MM-DD";
export default function useFormat(props: FormatProps): import("vue").ComputedRef<string>;
export {};
