"use strict";
var vue = require("vue");
function useIsMounted() {
  const isMounted = vue.ref(false);
  vue.onMounted(() => {
    isMounted.value = true;
  });
  return isMounted;
}
module.exports = useIsMounted;
