"use strict";
var __defProp = Object.defineProperty;
var __defProps = Object.defineProperties;
var __getOwnPropDescs = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues = (a, b) => {
  for (var prop in b || (b = {}))
    if (__hasOwnProp.call(b, prop))
      __defNormalProp(a, prop, b[prop]);
  if (__getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(b)) {
      if (__propIsEnum.call(b, prop))
        __defNormalProp(a, prop, b[prop]);
    }
  return a;
};
var __spreadProps = (a, b) => __defProps(a, __getOwnPropDescs(b));
Object.defineProperty(exports, "__esModule", { value: true });
exports[Symbol.toStringTag] = "Module";
var is = require("../_utils/is.js");
var vueUtils = require("../_utils/vue-utils.js");
var omit = require("../_utils/omit.js");
const travelSelectChildren = (children) => {
  var _a, _b, _c, _d;
  const options = [];
  for (const child of children) {
    if (!child)
      continue;
    if (vueUtils.isNamedComponent(child, "Optgroup")) {
      const props = vueUtils.resolveProps(child);
      let slots;
      let groupOptions = [];
      if (vueUtils.isSlotsChildren(child, child.children)) {
        slots = child.children;
        if (child.children.default) {
          groupOptions = travelSelectChildren(child.children.default());
        }
      } else if (vueUtils.isArrayChildren(child, child.children)) {
        groupOptions = travelSelectChildren(child.children);
      }
      options.push({
        isGroup: true,
        label: props.label,
        options: groupOptions,
        _props: props,
        _slots: slots
      });
    } else if (vueUtils.isNamedComponent(child, "Option")) {
      const props = vueUtils.resolveProps(child);
      let slots;
      let childrenString = "";
      let render;
      if (vueUtils.isSlotsChildren(child, child.children)) {
        slots = child.children;
        if (child.children.default) {
          render = child.children.default;
          childrenString = vueUtils.getChildrenString(child.children.default());
        }
      }
      options.push(__spreadProps(__spreadValues({}, props.extra), {
        value: (_a = props.value) != null ? _a : childrenString,
        label: (_b = props.label) != null ? _b : childrenString,
        render,
        disabled: props.disabled,
        tagProps: props.tagProps,
        _props: omit.omit(props, ["tagProps", "extra"]),
        _slots: slots
      }));
    } else if (vueUtils.isArrayChildren(child, child.children)) {
      options.push(...travelSelectChildren(child.children));
    } else if (vueUtils.isSlotsChildren(child, child.children)) {
      const _children = (_d = (_c = child.children).default) == null ? void 0 : _d.call(_c);
      if (_children) {
        options.push(...travelSelectChildren(_children));
      }
    } else if (is.isArray(child)) {
      options.push(...travelSelectChildren(child));
    }
  }
  return options;
};
const getTagData = (value, optionInfoMap) => {
  if (is.isUndefined(value)) {
    return void 0;
  }
  const optionInfo = optionInfoMap.get(value);
  if (!optionInfo) {
    return void 0;
  }
  return __spreadProps(__spreadValues({}, optionInfo), {
    label: optionInfo.label,
    closable: !optionInfo.disabled
  });
};
const getTagDataFromModelValue = (modelValue, optionInfoMap) => {
  if (is.isArray(modelValue)) {
    const result = [];
    for (const item of modelValue) {
      const valueData = getTagData(item, optionInfoMap);
      if (valueData) {
        result.push(valueData);
      }
    }
    return result;
  }
  return getTagData(modelValue, optionInfoMap);
};
exports.getTagDataFromModelValue = getTagDataFromModelValue;
exports.travelSelectChildren = travelSelectChildren;
