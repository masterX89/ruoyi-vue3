"use strict";
var vue = require("vue");
var baseSelect = require("./base-select.js");
var utils = require("./utils.js");
var omit = require("../_utils/omit.js");
const Select = (props, {
  slots
}) => {
  if (slots.default) {
    props.options = utils.travelSelectChildren(slots.default());
  }
  const omitSlots = omit.omit(slots, ["default"]);
  return vue.createVNode(baseSelect, props, omitSlots);
};
Select.displayName = "Select";
module.exports = Select;
